import React from "react";

class Button extends React.Component { //CLASS COMPONENT
    render(){
        return(
            <button>
                Botão
            </button>
        )
    }
}

export default Button;